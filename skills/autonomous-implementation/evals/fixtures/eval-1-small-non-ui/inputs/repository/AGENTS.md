# Fixture Repository Instructions

- Use the built-in Node.js test runner; do not add dependencies or a lockfile.
- Keep the project in ESM syntax.
- Change only files named by the current Task Contract.
- Run the exact scoped test and representative-flow commands before reporting.
