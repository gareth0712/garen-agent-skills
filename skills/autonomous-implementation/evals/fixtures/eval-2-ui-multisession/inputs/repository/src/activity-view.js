export function renderActivityPanel() {
  return `
    <section class="activity" aria-labelledby="activity-title">
      <h2 id="activity-title">Recent activity</h2>
      <p class="empty">No unread activity.</p>
    </section>
  `;
}
