# Fixture Instructions

- Repository revision: `fixture-csv-v1`.
- Planning is read-only.
- Server routes authorize through `requireTeamOwner`.
- Tests use `pnpm test`; type checks use `pnpm typecheck`.
- Production changes require separate implementation approval.
