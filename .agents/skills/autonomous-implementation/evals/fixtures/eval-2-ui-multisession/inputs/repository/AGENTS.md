# Fixture Repository Instructions

- Use ESM and the built-in Node.js test runner; add no dependencies or lockfiles.
- Preserve the existing dashboard header and account summary.
- Use semantic HTML and escape dynamic text.
- A localhost server is for verification only and must be stopped after use.
- Change only current Task Contract paths.
